import { OnCreateCheckDirective } from './on-create-check.directive';

describe('OnCreateCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new OnCreateCheckDirective();
    expect(directive).toBeTruthy();
  });
});
